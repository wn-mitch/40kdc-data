// Package wh40kdc is the Go port of the 40kdc-data dataset/engine, held in
// cross-language parity with the TypeScript, Rust, and Python implementations
// by the shared conformance corpus under conformance/.
package wh40kdc

// Version is the package version, kept in lockstep with tools/package.json,
// crates/wh40kdc/Cargo.toml, and python .../_version.py (CI fails on drift).
const Version = "0.5.17"
